"""
(LEGACY) Flat-text PyMuPDF extractor.

This module is no longer used now that the structured Phase 1 + Phase 2
pipeline is in place, but is kept as a reference. It can safely be removed
if you do not need the old `--- Page N ---` style flat text output anymore.
"""
